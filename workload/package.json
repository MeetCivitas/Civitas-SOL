{
  "name": "civitas-nilcc-workload",
  "version": "3.0.0",
  "description": "Civitas nilCC TEE workload — BN254 Poseidon merkle tree compute (no NilDB writes; orchestrator handles persistence)",
  "private": true,
  "type": "commonjs",
  "dependencies": {
    "node-fetch": "^2.7.0",
    "poseidon-lite": "^0.3.0"
  }
}
